import { defineConfig } from 'vite';

export default defineConfig({
    // Base path for GitHub Pages: https://<username>.github.io/Goop/
    base: '/Goop/',
    build: {
        outDir: 'dist',
    },
});
